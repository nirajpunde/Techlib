<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.niraj.learn">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/new_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login" />
        <activity android:name=".booklist" />
        <activity android:name=".user_area" />
        <activity android:name=".Contrib" />
        <activity android:name=".View_all" />
        <activity android:name=".BookDisplay" />
        <activity android:name=".user_contri" />
        <activity android:name=".book_manage" />
        <activity android:name=".Subject" />
        <activity android:name=".SubjectList" />
        <activity android:name=".list_of_sub"></activity>
    </application>

</manifest>